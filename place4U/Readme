To-do
1. https 설정 for 사용자 위치 접근
2. 지도 내 pin에 해당하는 infowindow 내용 설정(출력 오류)
3. DB 업데이트 

Done
- 위치 & 태그 검색
- 최근 검색어
- DB 설계
- CSV를 읽고, DB에 추가하기 위한 파이썬 코드(DB_update.py)
- 검색 결과 화면(지도 + 검색된 리스트에 해당하는 위치 좌표 지도에 pin 표시 + 전체 검색 리스트 출력)


명령어
python manage.py runserver :: localhost:8000으로 웹서버 실행
python manage.py runserver XXX.XXX.XXX.XXX:port :: 웹서버 주소 할당

#서버 실행후 {서버주소}/admin 접속한뒤 admin/admin입력으로 DB내용 확인 가능(SearchedTag, SearchedList table)
